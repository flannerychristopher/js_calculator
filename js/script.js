function add(a, b) {
	let answer = parseInt(a) + parseInt(b);
  return answer;
}
function subtract(a, b) {
  let answer = parseInt(a) - parseInt(b);
  return answer;
}
function divide(a, b) {
	parseInt(a) / parseInt(b)
}
function multiply(a, b) {
	parseInt(a) * parseInt(b)
}
